# Infra

This directory is reserved for infrastructure helpers that support local development and future deployment work.

Current root-level infra assets:

- `docker-compose.yml`
- `.env.example`
- `.gitignore`

Future additions can live here, such as:

- deployment manifests
- reverse proxy config
- database bootstrap scripts
- CI/CD helper files

